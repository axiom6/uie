// Generated by CoffeeScript 1.10.0
(function() {
  var $, NavigateUI;

  $ = require('jquery');

  NavigateUI = (function() {
    module.exports = NavigateUI;

    function NavigateUI(stream) {
      this.stream = stream;
    }

    NavigateUI.prototype.ready = function() {
      return this.$ = $(this.html());
    };

    NavigateUI.prototype.position = function(screen) {
      Util.noop(screen);
      return this.subscribe();
    };

    NavigateUI.prototype.subscribe = function() {
      return this.stream.subscribe('Screen', (function(_this) {
        return function(screen) {
          return _this.onScreen(screen);
        };
      })(this));
    };

    NavigateUI.prototype.onScreen = function(screen) {
      return Util.noop('NavigateUI.onScreen()', screen);
    };

    NavigateUI.prototype.html = function() {
      return "<div id=\"" + (Util.htmlId('NavigateUI')) + "\" class=\"" + (Util.css('NavigateUI')) + "\">Navigate</div>";
    };

    NavigateUI.prototype.show = function() {
      return this.$.show();
    };

    NavigateUI.prototype.hide = function() {
      return this.$.hide();
    };

    return NavigateUI;

  })();

}).call(this);
