// Generated by CoffeeScript 1.10.0
(function() {
  var $, SearchUC;

  $ = require('jquery');

  SearchUC = (function() {
    module.exports = SearchUC;

    function SearchUC(stream, role, port, land, dealsUC) {
      this.stream = stream;
      this.role = role;
      this.port = port;
      this.land = land;
      this.dealsUC = dealsUC;
    }

    SearchUC.prototype.ready = function() {
      return this.$ = $(this.html());
    };

    SearchUC.prototype.position = function(screen) {
      this.onScreen(screen);
      return this.subscribe();
    };

    SearchUC.prototype.subscribe = function() {
      this.stream.subscribe('Location', (function(_this) {
        return function(location) {
          return _this.onLocation(location);
        };
      })(this));
      return this.stream.subscribe('Screen', (function(_this) {
        return function(screen) {
          return _this.onScreen(screen);
        };
      })(this));
    };

    SearchUC.prototype.onLocation = function(location) {
      return Util.noop('SearchUC.onLocation()', this.ext, location);
    };

    SearchUC.prototype.onScreen = function(screen) {
      return Util.cssPosition(this.$, screen, this.port, this.land);
    };

    SearchUC.prototype.html = function() {
      return "<div id=\"" + (Util.htmlId('SearchUC', this.role)) + "\" class=\"" + (Util.css('SearchUC')) + "\"></div>";
    };

    return SearchUC;

  })();

}).call(this);
