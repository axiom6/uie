

Visual = require( 'js/visual/Visual' )
d3     = require( 'd3' )
c3     = require( 'c3' )

class C3V

  module.exports = C3Q # Util.Export( C3Q, 'visual/C3V' )

  constructor:( @stream ) ->

  chart:( obj ) ->
    Util.noop( obj )

  data:( obj ) ->
    Util.noop( obj )

  axis:( obj ) ->
    Util.noop( obj )

  grid:( obj ) ->
    Util.noop( obj )

  region:( obj ) ->
    Util.noop( obj )

  legend:( obj ) ->
    Util.noop( obj )

  tooltip:( obj ) ->
    Util.noop( obj )

  subchart:( obj ) ->
    Util.noop( obj )

  zoom:( obj ) ->
    Util.noop( obj )

  point:( obj ) ->
    Util.noop( obj )

  line:( obj ) ->
    Util.noop( obj )

  area:( obj ) ->
    Util.noop( obj )

  bar:( obj ) ->
    Util.noop( obj )

  pie:( obj ) ->
    Util.noop( obj )

  guage:( obj ) ->
    Util.noop( obj )
